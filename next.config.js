/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["billionaires.africa"],
    },
  //   unoptimized: true,
  //   output: 'export',
  };
  
  module.exports = nextConfig;
  